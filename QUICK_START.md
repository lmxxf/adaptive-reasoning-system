# 🚀 快速开始指南

你的DeepSeek API密钥已经配置完成，可以直接运行演示程序！

---

## ✅ API密钥配置方式

系统支持三种方式提供API密钥（按优先级）：

1. **环境变量**（推荐）
2. **交互式输入**（运行时提示输入）
3. **配置文件**（不推荐，已移除）

---

## 🔑 设置API密钥

### 方式1: 环境变量（推荐，最安全）

```bash
# 临时设置（仅当前终端会话）
export DEEPSEEK_API_KEY='sk-your-api-key-here'

# 或永久设置（添加到 ~/.bashrc）
echo 'export DEEPSEEK_API_KEY="sk-your-api-key-here"' >> ~/.bashrc
source ~/.bashrc
```

### 方式2: 运行时设置（一次性使用）

```bash
DEEPSEEK_API_KEY='sk-your-api-key-here' python3 run_demo.py
```

### 方式3: 交互式输入（没有环境变量时自动提示）

直接运行程序，系统会提示你输入API密钥：

```bash
python3 run_demo.py
# 程序会提示: 🔑 DeepSeek API Key: _____
```

---

## 🎯 一键运行演示

### 使用真实DeepSeek API

```bash
python3 run_demo.py
```

这将：
- ✅ 自动从环境变量加载API密钥（或提示输入）
- ✅ 使用DeepSeek API进行真实推理
- ✅ 展示4个不同复杂度的任务
- ✅ 显示自适应推理系统的选择过程

### 方式2: 运行模拟演示（无需API）

```bash
python3 demo.py
```

这将使用模拟响应，不消耗API配额。

### 方式3: 完整测试套件

```bash
python3 test_examples.py
```

运行所有测试用例，验证系统功能。

---

## 📦 安装依赖（如果还未安装）

### 快速安装

```bash
./install.sh
```

### 或手动安装

```bash
# 安装系统依赖
sudo apt install python3-venv

# 安装Python依赖
pip3 install -r requirements.txt
```

---

## 🎬 演示任务说明

`run_demo.py` 会测试以下4个任务：

1. **编程任务** - "编写一个Python函数计算两个数的最大公约数"
   - 预期: 非思考模式/简化思考模式
   - 特点: 结构化任务，直接输出

2. **数学证明** - "证明：对于任意正整数n，1+2+3+...+n = n(n+1)/2"
   - 预期: 完整思考模式
   - 特点: 需要逻辑推理和严密论证

3. **简单问答** - "什么是人工智能？"
   - 预期: 非思考模式
   - 特点: 知识检索，直接回答

4. **复杂设计** - "设计一个高并发的微服务架构"
   - 预期: 完整思考模式/简化思考模式
   - 特点: 需要多方面考虑和综合设计

---

## 📊 输出示例

运行 `python3 run_demo.py` 后，你会看到：

```
🤖 自适应推理系统 - DeepSeek API 演示
============================================================
✅ API密钥已加载: sk-e174793041...
✅ 系统初始化成功

🎯 开始测试 (共4个任务)...

============================================================
📝 任务 1/4: 编写一个Python函数计算两个数的最大公约数
💡 预期推理模式: 编程任务 → 非思考模式/简化思考模式

🧠 选择模式: NON_THINKING
📊 复杂度分数: 4.5
✅ 置信度: 85.5%
⏱️  执行时间: 1.234秒

💬 DeepSeek响应:
------------------------------------------------------------
def gcd(a, b):
    """计算两个数的最大公约数（辗转相除法）"""
    while b:
        a, b = b, a % b
    return a
------------------------------------------------------------

... (继续后续任务)
```

---

## 🔧 自定义配置

### 修改API密钥

只需要更新环境变量：

```bash
# 更新环境变量
export DEEPSEEK_API_KEY="your-new-api-key"
```

### 添加其他API

```bash
# 设置多个API密钥
export DEEPSEEK_API_KEY="sk-your-deepseek-key"
export OPENAI_API_KEY="sk-your-openai-key"
export ANTHROPIC_API_KEY="sk-ant-your-claude-key"
```

然后在代码中使用：

```python
from config import get_openai_api_key
from llm_integration_example import ProductionAdaptiveReasoningSystem

# 自动从环境变量获取（或提示输入）
api_key = get_openai_api_key()

system = ProductionAdaptiveReasoningSystem(
    api_type="openai",
    api_key=api_key
)
```

---

## 💡 使用技巧

### 在你的代码中使用

```python
from config import get_deepseek_api_key
from llm_integration_example import ProductionAdaptiveReasoningSystem

# 获取API密钥（自动从环境变量或提示输入）
api_key = get_deepseek_api_key()

# 初始化系统
system = ProductionAdaptiveReasoningSystem(
    api_type="deepseek",
    api_key=api_key
)

# 处理单个任务
result = system.process_task("你的问题", "task_id")
print(f"模式: {result.reasoning_mode.value}")
print(f"响应: {result.response}")

# 批量处理
tasks = [
    {"id": "1", "text": "任务1"},
    {"id": "2", "text": "任务2"}
]
import asyncio
results = asyncio.run(system.process_batch(tasks))
```

### 调整复杂度阈值

```python
system = ProductionAdaptiveReasoningSystem(
    api_type="deepseek",
    api_key=DEEPSEEK_API_KEY,
    config={
        "thresholds": {
            "non_thinking": 3.0,    # 低于3.0使用非思考模式
            "simplified": 6.0       # 3.0-6.0使用简化模式，高于6.0使用完整模式
        }
    }
)
```

---

## 📚 更多资源

- **项目文档**: `README.md`
- **系统架构**: `adaptive_reasoning_system.py`
- **LLM集成**: `llm_integration_example.py`
- **测试用例**: `test_examples.py`

---

## ⚠️ 注意事项

1. **API配额**: 使用真实API会消耗配额，注意控制测试次数
2. **Git安全**: `config.py` 已添加到 `.gitignore`，不会被提交
3. **网络要求**: 需要能访问 `api.deepseek.com`

---

## 🐛 常见问题

### Q: 运行时提示"未安装openai库"

A: 安装依赖：
```bash
pip3 install openai
```

### Q: API调用失败

A: 检查：
1. API密钥是否正确
2. 网络连接是否正常
3. DeepSeek API服务是否可用

### Q: 想使用模拟模式测试

A: 运行 `python3 demo.py` 或：
```python
system = ProductionAdaptiveReasoningSystem(api_type="simulation")
```

---

**准备好了吗？运行 `python3 run_demo.py` 开始体验！** 🚀
